{
  "name": "operator-ui",
  "private": true,
  "version": "0.1.0",
  "description": "Agent-harness operator dashboard — Preact SPA built with esbuild.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "preact": "^10.29.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@testing-library/preact": "^3.2.4",
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.4"
  }
}
